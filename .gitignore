# Build output
build/
build.log
build_*/
*.log

# Python venv for nRF Cloud utils
.venv-nrfcloud/

# nRF Cloud credentials: NEVER commit private keys (CA key + device key).
# Public certs / onboarding CSV are kept so the device can be rebuilt.
nrfcloud_creds/*_prv.pem

# Unzipped Edge AI model export (the .zip archive is committed instead)
Smart_Toilet_93499_wake_word/

# Local Claude Code settings
.claude/

# Compiled-in nRF Cloud device private key (secret)
app/certs/private-key.pem
